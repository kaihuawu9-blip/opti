'use client';

/**
 * 豪雅数字化手册 · 物理凸标侧栏（与 `ZeissSeriesNavList` 同源实现）。
 * 父级传入 `brand="hoya"` 与 `buildHandbookSeriesNavItemsForBrand('hoya')` 的项即可。
 */
export { ZeissSeriesNavList as HoyaSeriesNavList } from '@/components/catalog/ZeissSeriesNavList';
