'use client';

/**
 * 数字化手册 · 右侧系列导航（Matrix V1.3 · 品牌隔离）
 *
 * 数据源：`buildHandbookSeriesNavItemsForBrand`（依视路 classic 栅格）。
 * 蔡司/豪雅物理凸标导航已迁入 `ZeissHandbookPage` 右缘热区，主 UI 不再挂载 physical-tabs。
 */
export { ZeissSeriesNavList as HandbookSidebar } from '@/components/catalog/ZeissSeriesNavList';
